<h1><%= @patient.name %>'s Appointments</h1>
<ul>
<% @patient.appointments.each do |appt| %>
    <li><%= link_to appt.human_datetime, appointment_path(appt) %> with <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></li>
<% end %>
</ul>