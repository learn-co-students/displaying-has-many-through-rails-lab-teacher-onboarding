<h1><%= @doctor.name %>'s Appointments</h1>
<ul>
<% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.human_datetime, appointment_path(appt) %> with <%= link_to appt.patient.name, patient_path(appt.patient) %></li>
<% end %>
</ul>